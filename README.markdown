Compile
-------
* `tools/makefile.py`
* `make clean`
* `make`

Run (Qemu)
----------
* `make image`
* `make run`

(There is a short version for `make && make image && make run`: `make test`.

Run (GRUB)
----------
* Coming soon ;)
